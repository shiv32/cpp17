If setup is a **minimal NGINX install**, 
Then you don’t see `sites-available` / `sites-enabled`.
This is normal in some environments (WSL, custom builds, Arch, etc.)

---

# Easiest Fix (Use `conf.d` style)

Since your `/etc/nginx/` does NOT have site folders, do this:

## 1️⃣ Create a new config file

sudo nano /etc/nginx/myapp.conf

Paste:

server {
    listen 80;

    location /api/ {
        proxy_pass http://127.0.0.1:8080/;
    }
}

Save:

CTRL + O → Enter → CTRL + X

---

## 2️⃣ Check if NGINX loads extra configs

Open main config:

sudo nano /etc/nginx/nginx.conf

Look inside `http { ... }`

You should see something like:

include /etc/nginx/*.conf;

OR

include /etc/nginx/conf.d/*.conf;

---

## If NOT present → add this inside `http {}`:

include /etc/nginx/myapp.conf;

---

## 3️⃣ Test config

sudo nginx -t

---

## 4️⃣ Reload NGINX

sudo systemctl reload nginx

---

# 5️⃣ Test your setup

Run your C++ server on port `8080`, then:

http://localhost/api?key=test

---

# What’s different in your system?

Instead of:

sites-available → sites-enabled

Your system uses:

nginx.conf → include *.conf

👉 Simpler but less modular

---

# Pro Tip (Cleaner setup)

You can also create a folder:

sudo mkdir /etc/nginx/conf.d
sudo nano /etc/nginx/conf.d/myapp.conf

Then in `nginx.conf`:

include /etc/nginx/conf.d/*.conf;

---

# Final mental model

nginx.conf → loads → your myapp.conf → routes → C++ backend

---
sudo nginx -T | grep proxy_pass

Restart nginx
    sudo nginx -t
    sudo systemctl restart nginx