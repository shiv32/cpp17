If setup is a **minimal NGINX install**, 
Then you don’t see `sites-available` / `sites-enabled`.
This is normal in some environments (WSL, custom builds, Arch, etc.)

---

# Easiest Fix (Use `conf.d` style)

Since your `/etc/nginx/` does NOT have site folders, do this:

## 1️⃣ add in config file

sudo nano /etc/nginx/nginx.conf

Paste:

server {
    listen 80;

    location /api/ {
        proxy_pass http://127.0.0.1:8080/;
    }
}

Save:

CTRL + O → Enter → CTRL + X

---

## 3️⃣ Test config

sudo nginx -t
---

## 4️⃣ Reload NGINX
sudo systemctl reload nginx

---

# 5️⃣ Test your setup
Run your C++ server on port `8080`, then:

in browser
http://localhost/api?key=test

---
# What’s different in your system?

Instead of:

sites-available → sites-enabled

Your system uses:

nginx.conf 
---
# Final mental model

nginx.conf → loads → routes → C++ backend

---
To check proxy detail
    sudo nginx -T | grep proxy_pass

Restart nginx
    sudo nginx -t
    sudo systemctl restart nginx
    
Enable on boot
    sudo systemctl enable nginx

Disable on boot
    sudo systemctl disable nginx

Stop It Right Now (Optional)
    sudo systemctl stop nginx
Start    
    sudo systemctl start nginx

